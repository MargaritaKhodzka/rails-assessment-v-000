<h3>My categories:</h3>

<ul>
  <% if @categories %>
    <% @categories.each do |category| %>
    <li><strong><%= link_to category.title, category_path(category) %></strong></li>
    <% end %>
  <% else %>
    <h3>You don't have any categories yet</h3>
  <% end %>
</ul>
