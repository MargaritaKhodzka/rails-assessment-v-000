<h1><%= @category.title %> - <%= link_to 'Edit', edit_category_path(@category) %> - <%= link_to 'Delete', category_path(@category), method: :delete, data: { confirm: 'Are you sure you want to delete this category?' } %></h1>
<p>Climate: <%= @category.climate %></p>
<p>Must Have Items: <%= @category.must_have_items %></p>

<h3>See category destinations:</h3>
<ul><%= render(@destinations) || "There are no destinations in this category." %></ul>
