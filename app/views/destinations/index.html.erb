<h1>Destinations</h1>
<ul>
  <% @destinations.each do |destination| %>
    <li><%= link_to "Show #{destination.name}", destination_path(destination) %></li>
  <% end %>
</ul>

<%= link_to "New Destination", new_destination_path(@destination) %>
